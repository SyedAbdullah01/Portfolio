{
  "name": "abdullah-portfolio",
  "private": true,
  "version": "1.0.0",
  "description": "Local Tailwind CSS build for the portfolio site",
  "scripts": {
    "build:css": "tailwindcss -i ./tailwind-input.css -o ./tailwind.css --minify",
    "watch:css": "tailwindcss -i ./tailwind-input.css -o ./tailwind.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4",
    "tailwindcss": "^4"
  }
}
